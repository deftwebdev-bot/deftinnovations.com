# ============================================================
# Deft Innovations — Frontend environment (production template)
# Copy to frontend/.env.production (or set in your host's dashboard,
# e.g. Vercel → Project → Settings → Environment Variables).
# ============================================================

# Public origin of the Django API. NO trailing slash.
# Used both for data fetching and to scope next/image optimization.
NEXT_PUBLIC_API_URL=https://api.deftinnovations.com
